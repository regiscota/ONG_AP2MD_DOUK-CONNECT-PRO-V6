/* AP2MD-Douké CONNECT PRO — V3. Aucune clé secrète Supabase/Kobo ici. */
const AP2MD_CONFIG={supabaseUrl:"",supabasePublishableKey:""};
document.getElementById("year").textContent=new Date().getFullYear();
const counters=document.querySelectorAll("[data-counter]");let started=false;function animateCounters(){if(started)return;started=true;counters.forEach(el=>{const target=Number(el.dataset.counter);let n=0;const timer=setInterval(()=>{n+=Math.max(1,Math.ceil(target/45));if(n>=target){n=target;clearInterval(timer)}el.textContent=n+(target>=300?"+":"")},24)})}const stats=document.querySelector(".stats");if(stats)new IntersectionObserver(es=>{if(es[0].isIntersecting)animateCounters()},{threshold:.3}).observe(stats);const form=document.getElementById("loginForm"),msg=document.getElementById("loginMessage");form?.addEventListener("submit",e=>{e.preventDefault();msg.textContent="La connexion Supabase Auth sera activée après configuration de la clé publishable et de l'authentification."});document.getElementById("forgotBtn")?.addEventListener("click",()=>{msg.textContent="La récupération du mot de passe sera reliée à Supabase Auth dans l'étape de configuration."});
